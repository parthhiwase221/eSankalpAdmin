@using eSankalpAdmin.Models;
@model LoginModel

@{

    Layout = null;
}

            @*@Html.AntiForgeryToken()
            @Html.ValidationSummary(true)
            @if (@ViewBag.Message != null)
            {
                <div class="form-group">
                    @ViewBag.Message
                </div>
            }
            @using (Html.BeginForm("LogIn", "Login", FormMethod.Post))
            {
                <div class="input-group">
                    @Html.LabelFor(a => a.UserID)
                    @Html.TextBoxFor(a => a.UserID, new { @class = "form-control" })
                    @Html.ValidationMessageFor(a => a.UserID)
                </div>
                <div class="input-group">
                    @Html.LabelFor(a => a.Password)
                    @Html.PasswordFor(a => a.Password, new { @class = "form-control" })
           >*@


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-image: url('../../content/777.jpg');
            background-repeat: unset;
          /**//*  background-image: url('../../content/888.jpg';)*/
           /* background-image: url('../../content/999.jpg') ;*/
        }

        .container {
            display: flex;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            max-width: 900px;
            width: 100%;
        }

        .welcome-section {
            padding: 40px;
            background: linear-gradient(135deg, #2a5298, #1e3c72);
            color: white;
            width: 50%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .welcome-section img {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
        }

        .login-section {
            padding: 40px;
            width: 50%;
        }

        .login-section h2 {
            margin-bottom: 20px;
            font-size: 1.8rem;
        }

        .input-group {
            margin-bottom: 20px;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .btn-info {
            display: block;
            width: 100%;
            padding: 10px;
            background: #1e3c72;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
        }

        .btn-info:hover {
            background: #2a5298;
        }

        .forgot-password {
            text-align: right;
            margin-top: 10px;
        }

        .forgot-password a {
            color: #2a5298;
            text-decoration: none;
        }

        .forgot-password a:hover {
            text-decoration: underline;
        }

      

            .welcome-section, .login-section {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="welcome-section">
            <img src="~/Content/dist/img/ph33.jpg" alt="AdminLTE Logo" class="brand-image img-circle elevation-3" style="opacity: .9;margin-top:30px; margin-left:10px; height:180px; width:280px; ">
            <br />
            <br />
            <h2>Welcome to eSankalp</h2>

        </div>
        <div class="login-section">
            <h2>Sign In</h2>
            <form action="/Login/LogIn" method="post">
                <div class="input-group">
                    <label for="UserID">User ID</label>
                    <input type="text" id="UserID" name="UserID" class="form-control" required>
                </div>
                <div class="input-group">
                    <label for="Password">Password</label>
                    <input type="password" id="Password" name="Password" class="form-control" required>
                </div>
                <input type="submit" value="Login" class="btn-info">
                <div class="forgot-password">
                    <a href="#">Forgot Password?</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>

<!-- Let me know if you want any adjustments! -->
